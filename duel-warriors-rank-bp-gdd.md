# 《Duel Warriors》Rank PvP BP 系统 — 策划案(GDD)

> 版本:v1.1 | 状态:定稿(全部决策已确认)
> 适用范围:Rank 模式 1v1 / 2v2
> 配套文档:`duel-warriors-rank-bp-design.md`(技术实现层,规则以本 GDD 为准)

---

## 1. 设计目标

1. **策略前置**:在 Rank 对局开始前通过 Ban/Pick 形成博弈(针对超模技能、阵容克制),提升对局多样性与公平性。
2. **不拖节奏**:BP 总前置控制在 18~23s,双方提前确认可跳过倒计时,对局本身节奏不变。
3. **不阻塞、可挂机**:任何阶段超时都自动兜底(自动 ban / 自动 pick),断线不卡住对局。
4. **服务端权威**:所有提交、校验、兜底均由服务端完成,客户端只做表现,防刷分、防偷窥。

## 2. 总体流程与时长

```
玩家进入 Rank(匹配场景)
   → 匹配成功,服务端创建 Match,下发 BP 数据
   → Ban 界面加载(技能放置位置、ban 方式)          ← 全屏 UI,玩家仍停留在匹配场景
   → Ban 流程(双方同时盲ban,无先后顺序;结算按 蓝方→红方 固定顺序)
   → Ban 阶段结束,Reveal 结算展示(3s)
   → Pick 界面加载
   → Pick 流程(3 组×2 = 6 槽,组1 → 组2 → 组3 顺序选择)
   → 双方完成 → 加载界面 → 集体传送至战斗场景(服务端装配 loadout)
   → 战斗开始(节奏与普通对局一致)
```

**时长总表(暂定值,上线后按数据调整)**

| 阶段 | 1v1 | 2v2 | 备注 |
|---|---|---|---|
| Ban | 5s | 10s | 2v2 需队内协调,给更长 |
| Reveal | 3s | 3s | 固定 |
| Pick | 10s | 10s | — |
| **合计** | **18s** | **23s** | 不含场景加载 |

**EarlyReady**:本阶段所有玩家都已提交 → 立即结束该阶段剩余倒计时(保证节奏)。

## 3. 技能池与布局

- **Ban 池 / Pick 池 = 全部技能**(包含未拥有的;ban 不要求拥有该技能,pick 才要求)。
- **默认布局:单列表按等级升序排列——等级越高放下面**(低等级在上、高等级在底部)。
- **备选布局:金/紫分区**——金区在上、紫区在下,区内按等级降序(配置切换,仅影响客户端排列)。
- 卡片元素:技能图标、名称、等级、稀有度边框/角标(紫/金)。

## 4. Ban 规则

### 4.1 通用规则(两种模式一致)

- **同时盲ban**:各方独立选择,互相不可见(2v2 队友实时可见,见 4.3)。
- **重叠合法**:不同玩家可以 ban 同一技能;被 ban 技能卡片挂多个 ban 者头像。
- **空ban 合法**:玩家可以放弃槽位(手动提交 0 个)。
- **提交即锁定**:每人 `banPerPlayer` 个槽,提交后不可修改。
- 同一玩家自己的槽位内禁止重复 ban 同一技能(服务端拒绝)。
- 超时未提交的槽位按 4.2/4.3 的自动 ban 规则补满。

### 4.2 1v1

| 项 | 规则 |
|---|---|
| ban 数量 | 每队 2 个;**每个玩家可 ban 2 个**(总提交 4) |
| 时间 | 5s |
| 超时 | **自动选择布局顺序前 2 个技能为 ban 位**(允许重叠,严格前 N 个,不做跳过) |
| 可见性 | 完全盲ban,结算前互不可见 |

### 4.3 2v2

| 项 | 规则 |
|---|---|
| ban 数量 | 每队 2 个;**每个玩家可 ban 1 个**(总提交 4) |
| 时间 | 10s |
| 超时 | **自动选择布局顺序前 1 个技能为 ban 位** |
| 可见性 | 盲ban,但**队友实时可见**(队内预览显示队友头像);敌方隐藏 |

### 4.4 ban 后技能状态

- 被 ban 技能 = **灰 + ban 者头像栈**。
- 头像栈边界:**1v1 最多 2 个;2v2 最多 4 个**(重叠 ban 时叠加,按提交顺序排列;玩家总数即上限,服务端无需额外裁剪)。
- 自己/队友已勾选(结算前)的卡片:高亮 + 自己头像 / 队友头像(2v2)。

## 5. Pick 规则

### 5.1 通用规则(两种模式一致)

- Pick 池展示 = 全部技能;可选集 = 拥有 且 未被 ban。
- **技能状态**:

| 状态 | 表现 |
|---|---|
| 已拥有 | 高亮 |
| 未拥有 | 灰 + 🔒 |
| 被 ban | 灰 + ban 者头像栈(1v1 ≤2 / 2v2 ≤4) |
| 已 pick | **√**(叠加在已拥有状态上) |

- **优先级**:被 ban > 未拥有(🔒) > 已拥有(高亮);√ 与高亮叠加。被 ban 的卡不再显示 🔒。
- **Pick 数量:每人装配 6 个技能,分 3 组(每组 2 个技能槽)**。
- **Pick 顺序:3 组技能顺序选择**——组1(槽1-2)→ 组2(槽3-4)→ 组3(槽5-6),已选卡片显示 √ + 槽位序号。
- **空位自动顺序补全**:确认时若不满 6 个,剩余空位按布局顺序从可选集自动补全(跳过未拥有/被ban/已选)。
- 拥有 + 未被 ban 的技能不足 6 个时,允许空槽(战斗侧按实际技能数适配)。
- **对方永远收不到你的 loadout**(防插件偷窥);2v2 队友确认后可见,镜像允许(仅提示)。

### 5.2 1v1

| 项 | 规则 |
|---|---|
| 时间 | 10s |
| 超时 | 自动选择布局顺序前 6 个技能为 pick 位 |
| 可见性 | 互不可见 |

### 5.3 2v2

| 项 | 规则 |
|---|---|
| 时间 | 10s |
| 超时 | 同 1v1:自动前 6 个 |
| 可见性 | 队友确认后可见(TeammatePick 队内广播),敌方不可见 |
| 镜像 | 允许,队内显示"队友已选"提示 |

## 6. 超时兜底规则(汇总)

| 阶段 | 1v1 | 2v2 |
|---|---|---|
| Ban 超时 | 自动 ban 布局顺序**前 2** 个 | 自动 ban 布局顺序**前 1** 个 |
| Pick 超时 | 自动 pick 布局顺序**前 6** 个 | 同左 |

**自动补全统一算法**(ban 与 pick 共用):

```
AutoFill(玩家, 当前已选, 目标数量):
  候选 = 按布局顺序的技能列表
  依次遍历候选:
    ban:跳过本玩家已选;允许与其他玩家重叠(严格前 N 个,不做跳过)
    pick:跳过 未拥有 / 被ban / 已选
    选中填入空槽,直到达到目标数量
  不足 6 个时允许空槽(仅 pick)
```

- 部分提交 + 超时:剩余槽位用同一算法补满(如 1v1 手动 ban 了 1 个就超时,自动补第 2 个)。
- 自动补全的槽位在 UI 上加"自动"标记,便于玩家理解。

## 7. 服务端状态机(技术摘要)

详见 `duel-warriors-rank-bp-design.md`。核心:

```
BAN(同时盲ban,计时) → BAN_REVEAL(3s,广播 bannedBy 头像映射) → PICK(顺序选择,计时) → BATTLE
```

- 权威数据:`banSubmissions`、`bannedBy`(skillId → 头像 userId 列表)、`pickSubmissions`
- 校验:阶段白名单、槽位上限、技能存在性、拥有校验(DataStore)、本玩家 ban 槽不重复、loadout 内不重复、被 ban 不可 pick
- 兜底:`resolveBans()`、`AutoFill()`、默认 loadout;全部服务端定时器驱动
- Remote:单 Remote + action 枚举(`SubmitBan` / `SubmitPick` / `EarlyReady`);服务端按需 `SendTo` / `SendToTeam` / `BroadcastAll`

## 8. 场景与加载流程

1. BP 全程在**匹配场景**内完成,以全屏 UI 呈现,不切换场景。
2. 双方 Pick 完成(或超时兜底)→ 服务端为每人装配最终 loadout → 客户端显示加载界面 → `TeleportService` 集体传送至战斗场景。
3. 战斗场景内所有玩家就绪后才开始战斗,保证对局节奏与普通对局一致。

## 9. UI 规格(卡片状态总表)

| 阶段 | 状态 | 表现 |
|---|---|---|
| Ban | 可选 | 正常图标(紫框/金框 + 等级) |
| Ban | 自己已勾选 | 高亮 + 自己头像 |
| Ban(2v2) | 队友已勾选 | 队友头像(仅队内可见) |
| Reveal | 被 ban | 灰 + ban 者头像栈(1v1 ≤2 / 2v2 ≤4) |
| Pick | 已拥有 | 高亮 |
| Pick | 未拥有 | 灰 + 🔒 |
| Pick | 被 ban | 灰 + ban 者头像栈 |
| Pick | 已 pick | √ + 槽位序号 1~6(组1/组2/组3) |
| 兜底 | 自动补全 | 卡片角标"自动" |

## 10. 边界与异常处理

| 场景 | 处理 |
|---|---|
| 重叠 ban | 合法,头像栈叠加(≤2 / ≤4) |
| 空 ban(手动放弃) | 合法,视为放弃槽位 |
| 同一玩家重复 ban 同一技能 | 服务端拒绝并提示 |
| 重复提交 / 阶段外提交 | 服务端拒绝 + 审计日志 |
| 伪造技能 id / 选未拥有技能 | 服务端按 SkillCatalog + DataStore 拒绝 |
| ban 阶段掉线 | 1v1:判负;2v2:该槽记空 ban |
| pick 阶段掉线 / 超时 | 自动补全 loadout(拥有不足 6 允许空槽) |
| 双方都超时 ban | 按 P1 决策处理重叠 |
| 加载/传送中途断线 | 掉线方判负,对手直接获胜(不等待) |

## 11. 验收清单(QA 用例)

1. 匹配成功后 1s 内弹出 BP 全屏 UI,技能按等级升序(高等级在底部),紫/金稀有度框正确。
2. 1v1 Ban 5s 倒计时,双方互相看不到对方选择;提交后显示"已确认,等待对手"。
3. 超时:未操作玩家自动 ban 前 2 个(1v1)/ 前 1 个(2v2),UI 有"自动"标记。
4. 重叠 ban:同一技能卡片显示 2 个(1v1)/ 最多 4 个(2v2)ban 者头像;双方都超时自动 ban 时同样允许自动 ban 到同一技能。
5. 自己两个槽 ban 同一技能 → 被拒绝并提示。
6. 手动空 ban:允许,该玩家不产生 ban 记录。
7. Reveal 3s 后自动进入 Pick;被 ban 卡片为灰+头像。
8. Pick 卡片状态:已拥有高亮 / 未拥有灰+🔒 / 被ban灰+头像,优先级正确(被ban 不叠加🔒)。
9. 顺序选择:按 组1→组2→组3 顺序,已选技能显示 √ + 槽位序号(1~6);点第 7 个时提示已满。
10. 确认时缺槽(不满 6):空位自动顺序补全,补的卡片带"自动"标记。
11. Pick 超时:自动选择布局顺序前 6 个技能填位,每人装配 6 个。
12. 对方 loadout 全程不可见;2v2 队友确认后可见。
13. 双方完成 → 集体传送战斗场景,全员就绪后才开战。
14. 各阶段断线/退出:按第 10 节处理,不卡住对局。
15. 客户端伪造请求(阶段外提交、非法 id、超槽位)全部被服务端拒绝。

## 12. 已决议事项(历史决策)

| # | 问题 | 决议 |
|---|---|---|
| P1 | 超时自动 ban 的重叠处理 | **允许重叠**——严格按布局顺序取前 N 个,不做跳过;所有玩家可以 ban 同一技能,头像栈叠加展示 |
| P2 | Pick 超时"前 6 个"的语义 | **每人装配 6 个技能(3 组×2)**——超时自动选择布局顺序前 6 个填位 |

---

### 变更记录

- v1.0:初稿,整合全部已确认决策(同时盲ban / 每队2 / 灰+头像栈 / 等级升序布局 / √ 状态 / 空位补全 / 匹配场景集成)。
- v1.1:按决议更新——P1 自动 ban 允许重叠;P2 每人装配 6 个技能(3组×2);Pick 超时自动前 6 个。
